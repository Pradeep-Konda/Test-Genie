info:
  title: Generated API
  version: 1.0.0
openapi: 3.0.0
paths:
  /health:
    get:
      responses:
        '200':
          description: Success
      summary: Check if the service is healthy.
  /users:
    get:
      responses:
        '200':
          description: Success
      summary: Create a new user.
  /users/<int:user_id>:
    get:
      responses:
        '200':
          description: Success
      summary: Get details of a user by ID.
